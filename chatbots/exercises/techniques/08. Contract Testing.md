## Contract Testing

It formalizes integration testing of a Consumer/Provider setup.

1. Static Testing of the Contract.
2. Running contract tests for both ends.

Check `test_contract_testing.py` in `demos` directory.